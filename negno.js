function makeNegative(number) {
    return -number;
  }
  
  const positiveNumber = 42;
  const negativeResult = makeNegative(positiveNumber);
  
  console.log(`Negative of ${positiveNumber} is ${negativeResult}`);
  